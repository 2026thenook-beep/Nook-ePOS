# Test report — The Nook ePOS 2.0.3

The release verification script and all static/regression test files must pass before packaging.

Checks include version consistency, API centralisation, shared busy-overlay service, all historical feature tests and database-repair tests.
