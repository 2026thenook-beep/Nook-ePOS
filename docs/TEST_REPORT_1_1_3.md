# Test Report — 1.1.4

All existing Node-based static and regression tests passed.

Additional checks verify:
- the menu item grid has its own scroll container;
- the ticket content has a separate scroll container;
- fixed ticket metadata remains outside the scrolling ticket content;
- standard prompt choices use full-card labels;
- variable quantity prompt choices retain separate quantity controls;
- frontend and backend release identifiers are 1.1.4.
