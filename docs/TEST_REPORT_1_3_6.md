# Test Report 1.3.7

Validated:

- Prompt move-up and move-down controls render in Menu Admin.
- Prompt option move-up and move-down controls render in Menu Admin.
- Reordering normalises Sort values and persists each affected record through the existing Google Sheets save actions.
- New prompts are assigned after the current highest prompt Sort value for that item.
- New options are assigned after the current highest option Sort value for that prompt.
- Existing frontend and backend syntax checks pass.
- Complete regression suite passes.
